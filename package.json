{
  "name": "idea-plot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-packager . idea-plot --overwrite --asar=true --platform=linux --arch=x64 --prune=true --out=dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ag-grid": "^18.1.2"
  },
  "devDependencies": {
    "electron": "^3.0.10",
    "electron-packager": "^12.2.0"
  }
}
